# 2026-07-31 只读维护审查：潜在 Bug 清单复核与整合

```json
{
  "log_id": "worklog-20260731-001",
  "timestamp": "2026-07-31T18:30:00+08:00",
  "actor": "maintenance-agent",
  "task": "只读维护检查潜在 bug；复核 2026-07-31 早些时候 cline 会话的审查结果（实测复现三个高优先级问题），并完成其因 CRLF 行尾匹配失败而中断的 bugissue.md 整合写入",
  "affected_files": [
    "bugissue.md",
    "src/bookhub/library/formats/cbz.py",
    "src/bookhub/library/repository.py",
    "src/bookhub/library/scanner.py",
    "src/bookhub/ui/web_window.py",
    "src/bookhub/ui/web_bridge.py",
    "src/bookhub/ui/viewmodels/library_viewmodel.py",
    "src/bookhub/ui/web/js/app.js"
  ],
  "mode": "read_only（仅更新 bugissue.md 文档，未改动任何 src/ 代码）",
  "findings_verified": [
    "BUG-1 高/安全：cbz._safe_archive_member_path 在 Windows 上对 `/evil.jpg` 误判非绝对路径，cache_dir 拼接逃逸到盘根（zip-slip）——实测复现 `C:\\evil.jpg`",
    "BUG-2 高/数据丢失：remove_root/remove_comic_root/remove_text_root 的 `path LIKE ?` 未转义 `%`/`_`，路径含通配符时误删兄弟目录——实测复现 `100%_Special` 误伤 `1000XSpecial`",
    "BUG-3 高/UI 残留：library_viewmodel.search_suggestions_for_query 硬编码 'Bauhaus principles' 演示建议，空查询也返回",
    "BUG-4 中：scanner._copy_or_downscale_comic_placeholder PIL 解码失败仍 copy 原图",
    "BUG-5 中：scan_roots total_files=0 + max(current,total) → 扫描进度恒 100%",
    "BUG-6 中：web_window.apply_setting 对 scanDepth/textPreviewChars/comicPageSize/viewportBufferScreens/gridColumns 直接 int(value) 无容错",
    "BUG-7 中：repository._connection 有 busy_timeout 但无 WAL，多 worker 并发写库",
    "BUG-8 中/磁盘泄漏：cbz._cbz_read_cache_dir token 含 st_mtime_ns，历史 read 缓存目录永不清理",
    "低-1~5：_comic_page_payload 死代码 / 右键切换收藏空回调 / saveTextRules scanned 误报 / lastKey 缓存过期卡片 / _find_book_row 全表扫描"
  ],
  "baseline_tests": "用 .venv 运行部分回归测试 58 passed（test_library_viewmodel_search/test_comic_search/test_app_paths/test_rule_engine/test_text_rule_structure_parser）；系统 Python 跑测试缺 natsort（环境问题，requirements.txt 已声明）",
  "next_steps": [
    "优先修复 BUG-1（安全，建议先修）与 BUG-2（数据丢失）",
    "BUG-3 一行删除即可",
    "修复后运行 src/tests 全量回归并在 bugissue.md 打勾、追加留档"
  ],
  "risks": [
    "BUG-2 属于数据删除路径，修复前如用户库中已存在含 % 或 _ 的根目录路径，历史误删可能已发生",
    "BUG-1 修复（拒绝 / 前缀成员）可能影响极少数以根路径命名成员的非标准 CBZ",
    "本次仅文档更新，src/ 代码未变；修复需另行开工"
  ]
}
```

---

## 2026-07-31 修复 BUG-1：CBZ 成员路径穿越（zip-slip）

```json
{
  "log_id": "worklog-20260731-002",
  "timestamp": "2026-07-31T18:45:00+08:00",
  "actor": "maintenance-agent",
  "task": "修复 BUG-1（高优先级）：_safe_archive_member_path 在 Windows 上对 `/evil.jpg` 误判非绝对路径导致 cache_dir 拼接逃逸（zip-slip）",
  "affected_files": [
    "src/bookhub/library/formats/cbz.py",
    "src/tests/test_new_formats_import.py",
    "bugissue.md"
  ],
  "implementation": [
    "cbz.py `_safe_archive_member_path` 校验条件由 `is_absolute()` 扩展为 `is_absolute() or drive or root`：Windows 下 `Path('/x')` 无盘符 is_absolute 为 False，但 `.root == '\\\\'`，`/`、`\\`、盘符前缀成员一律返回 None",
    "函数签名与返回类型不变，3 个调用点（_cbz_read_cache_is_valid / _extract_cbz_members_to_cache / prepare_cbz_for_external_viewer）无需改动",
    "test_new_formats_import.py 新增 2 个用例：直接单测 6 种穿越向量 + 恶意 CBZ（writestr('/evil.png')）端到端断言返回 None 且无逃逸文件"
  ],
  "tests": [
    "pytest tests/test_new_formats_import.py -q → 11 passed, 6 subtests passed",
    "pytest tests -q 全量 → 179 passed, 32 subtests passed"
  ],
  "risks": [
    "拒绝 `/` 前缀成员可能影响极少数以根路径命名成员的非标准 CBZ（此类成员本就异常）",
    "不处理 Windows 保留设备名（CON/NUL）与 ADS 冒号成员：前者写入 OSError 兜底、后者不逃逸目录，属极罕见攻击面"
  ],
  "next_steps": [
    "修复 BUG-2（LIKE 通配符误删兄弟目录，数据丢失）",
    "BUG-3 一行删除硬编码建议"
  ]
}
```

---

## 2026-08-01 修复 BUG-2：删除根目录时 SQL LIKE 通配符误删兄弟目录

```json
{
  "log_id": "worklog-20260801-001",
  "timestamp": "2026-08-01T13:50:00+08:00",
  "actor": "maintenance-agent",
  "task": "修复 BUG-2（高优先级，数据丢失）：remove_root/remove_comic_root/remove_text_root 的 `path LIKE ?` 未转义 %/_ 通配符，Windows 目录名含通配符时误删兄弟目录书籍",
  "affected_files": [
    "src/bookhub/library/repository.py",
    "src/tests/test_repository_orphan_cleanup.py",
    "bugissue.md"
  ],
  "implementation": [
    "三处删除根方法（remove_root 848 行 / remove_comic_root 908 行 / remove_text_root 967 行）的 `path LIKE ?` 全部改为 `substr(path, 1, length(?)) = ?` 前缀精确比较",
    "`root_prefix` 变量由 `normalized + os.sep + \"%\"` 改为纯前缀 `normalized + os.sep`，SQL 参数由 2 个变 3 个",
    "选型理由：LIKE ESCAPE 需两处改动且转义易错；范围比较 `>= AND <` 在 root 含 `_` 时仍会误伤兄弟；substr 无通配符语义，与 Python 层 `_path_in_roots` 的 startswith 语义一致",
    "test_repository_orphan_cleanup.py 新增 RepositoryRootRemovalSafetyTests 类 3 个用例：Library/Comic/Text 三类型各验证含 100%_Special 根删除时兄弟 1000XSpecial 保留"
  ],
  "tests": [
    "pytest tests/test_repository_orphan_cleanup.py -q → 6 passed",
    "独立脚本对比：旧 LIKE 命中 2 条（误删兄弟）、新 substr 命中 1 条（只删目标根）",
    "pytest tests -q 全量 → 182 passed, 32 subtests passed"
  ],
  "risks": [
    "substr 前缀比较不走索引，但删除操作本就全表扫描、书库量级可控，可接受",
    "仅改删除路径；扫描侧 _remove_missing_books_in_scope 走 Python 层 _path_in_roots（startswith）本无此问题",
    "历史误删无法恢复（无备份时），已在 bugissue.md 风险注明"
  ],
  "next_steps": [
    "修复 BUG-3（删除 library_viewmodel 硬编码 'Bauhaus principles' 建议）",
    "后续：BUG-6/BUG-7（apply_setting int 容错 + WAL）、BUG-8（CBZ 缓存清理）"
  ]
}
```

---

## 2026-08-01 修复 BUG-3：搜索建议硬编码 "Bauhaus principles"

```json
{
  "log_id": "worklog-20260801-002",
  "timestamp": "2026-08-01T14:00:00+08:00",
  "actor": "maintenance-agent",
  "task": "修复 BUG-3（高优先级，UI 残留）：search_suggestions_for_query 空查询也返回硬编码 History/Bauhaus principles 演示建议",
  "affected_files": [
    "src/bookhub/ui/viewmodels/library_viewmodel.py",
    "src/tests/test_library_viewmodel_search.py",
    "bugissue.md"
  ],
  "implementation": [
    "search_suggestions_for_query 的 suggestions 初始值由含硬编码 History 条目的列表改为空列表 []",
    "test_library_viewmodel_search.py 新增 test_empty_query_has_no_hardcoded_history_suggestion：空查询无 History 组且无 'Bauhaus principles' 建议"
  ],
  "tests": [
    "pytest tests/test_library_viewmodel_search.py -q → 4 passed",
    "pytest tests -q 全量 → 183 passed, 32 subtests passed"
  ],
  "risks": [
    "搜索历史功能本就未实现，删除演示条目后 History 组不再出现，属预期行为",
    "如未来接入真实搜索历史，需自行持久化（当前无此存储）"
  ],
  "next_steps": [
    "后续：BUG-6/BUG-7（apply_setting int 容错 + WAL）、BUG-8（CBZ 缓存清理）、BUG-4/BUG-5"
  ]
}
```

---

## 2026-08-01 修复 BUG-4：超大漫画封面降采样失败仍复制原图

```json
{
  "log_id": "worklog-20260801-003",
  "timestamp": "2026-08-01T14:05:00+08:00",
  "actor": "maintenance-agent",
  "task": "修复 BUG-4（中优先级）：_copy_or_downscale_comic_placeholder 解码失败分支从无条件 copy 原图改为生成固定灰色占位 PNG，避免超大/损坏封面进入 Qt 解码路径（256MB 限制崩溃/白屏）",
  "affected_files": [
    "src/bookhub/library/scanner.py",
    "src/tests/test_comic_preview_pipeline.py",
    "bugissue.md"
  ],
  "implementation": [
    "新增常量 _PLACEHOLDER_FALLBACK_SIZE=(96,144) / _PLACEHOLDER_FALLBACK_COLOR=(112,118,128)",
    "except 分支不再 shutil.copy2 原图，改用 Image.new 生成固定尺寸灰色 PNG（不依赖解码外部文件，损坏/超大源也能成功），返回路径仍为 variant=original",
    "快扫占位 + 后台补全架构不变；两个调用点（文件夹 598-618 / CBZ 714-742）无需改动；must_rasterize（GIF/BMP/TIFF）路径照旧抛 OSError",
    "后台 _render_comic_thumbnail 对损坏源失败时 fallback 保留占位 URI（thumbnail_tasks.py 现有逻辑），UI 始终显示安全小图"
  ],
  "tests": [
    "test_comic_preview_pipeline.py 新增 2 个用例：test_broken_cover_placeholder_is_safe_small_png（非法字节封面扫描后占位为可解码小 PNG、非原图副本）、test_broken_cover_regenerate_falls_back_to_placeholder（regenerate 失败后 thumbnail_path 保留原占位 URI）",
    "pytest tests/test_comic_preview_pipeline.py -q → 8 passed",
    "pytest tests -q 全量 → 185 passed, 32 subtests passed"
  ],
  "risks": [
    "损坏/超大封面 UI 显示为灰色固定占位（不崩溃、无白屏）；后台 regenerate 每次 only_missing 扫描会重试真实源，文件修复后自动恢复真实封面",
    "占位 PNG 无任何原始视觉信息，属可接受的降级体验"
  ],
  "next_steps": [
    "修复 BUG-6/BUG-7（apply_setting int 容错 + WAL，可同批）",
    "修复 BUG-8（CBZ 读取缓存目录清理）、BUG-5（扫描进度恒 100%）"
  ]
}
```

---

## 2026-08-01 补充 .gitignore：忽略 pytest 输出与运行期扫描日志

```json
{
  "log_id": "worklog-20260801-004",
  "timestamp": "2026-08-01T13:57:00+08:00",
  "actor": "maintenance-agent",
  "task": "将遗留未跟踪文件 pytest_out*.txt 与 Scan_error_logs/*.txt 加入 .gitignore，清理 git status 噪音",
  "affected_files": [
    ".gitignore"
  ],
  "implementation": [
    "新增 `pytest_out*.txt`：忽略 pytest 重定向输出（pytest_out.txt / pytest_out2.txt / pytest_out3.txt 等）",
    "新增 `Scan_error_logs/`：目录模式匹配任意层级，同时覆盖 dev 态 src/Scan_error_logs 与打包态 exe 同级目录",
    "git check-ignore 验证：5 个目标文件全部命中；git status 未跟踪列表清空该两类"
  ],
  "risks": [
    "Scan_error_logs/ 下 7 个历史日志（20260517~20260724）已被 git 跟踪，gitignore 不影响已跟踪文件；未来如需解跟踪须手动 git rm --cached",
    "新扫描日志不再纳入版本控制，留档仅靠 Agent-rule/logs/history（符合 AGENTS.md 规范）"
  ],
  "next_steps": [
    "修复 BUG-6/BUG-7（apply_setting int 容错 + WAL，可同批）",
    "修复 BUG-8（CBZ 读取缓存目录清理）、BUG-5（扫描进度恒 100%）"
  ]
}
```

---

## 2026-08-01 修复 BUG-5：Library 扫描进度改 busy 态 + BUG-6：整型设置统一容错

```json
{
  "log_id": "worklog-20260801-005",
  "timestamp": "2026-08-01T18:35:00+08:00",
  "actor": "maintenance-agent",
  "task": "修复 BUG-5（中优先级，进度体验）：Library 扫描 total=0 被 max(current,total) 顶满导致进度恒 100%，改为透传 total=0 触发前端 busy 条纹动画；同步修复 BUG-6（健壮性）：apply_setting 5 处 int(value) 无容错，改 repository 层统一 normalize",
  "affected_files": [
    "src/bookhub/library/scanner.py",
    "src/bookhub/ui/web/js/app.js",
    "src/bookhub/ui/web/css/skins/glass/components.css",
    "src/bookhub/ui/web/css/skins/vaporwave/components.css",
    "src/bookhub/library/repository.py",
    "src/bookhub/ui/web_window.py",
    "src/tests/test_scan_pdf_degrade.py",
    "src/tests/test_repository_orphan_cleanup.py",
    "bugissue.md"
  ],
  "implementation": [
    "BUG-5：scanner.py `_emit_scan_progress` 由 `max(current, total)` 改为 `total if total > 0 else 0`，Library 扫描（无预遍历计数）total=0 如实透传；comic/text 扫描 total 恒 > current 不受影响",
    "BUG-5：app.js `updateScanProgress` 在 total<=0 时给进度条加 busy class 并显示「Scanning... · label」，total>0 走原百分比逻辑；`updateScanState` 停止时移除 busy class",
    "BUG-5：glass/vaporwave 两个皮肤 CSS 新增 `.progress.busy span` 条纹 + `progress-busy` keyframes 动画（皮肤各自变量）",
    "BUG-6：repository `set_scan_depth`（回退 2 / clamp 1-3）、`set_text_preview_chars`（回退 DEFAULT_TEXT_PREVIEW_CHARS / 白名单校验）补 try/except，签名放宽 `int | str | None`；comicPageSize/viewportBufferScreens/gridColumns 复用既有 _normalize_*",
    "BUG-6：web_window `apply_setting` 5 处 `int(value)` 全部去掉，原始 value 直传 repository 由其内部 normalize"
  ],
  "tests": [
    "test_scan_pdf_degrade.py：`test_scan_roots_reports_progress` 改名为 busy 语义并断言 total==0；新增 test_text_scan_reports_real_total / test_comic_scan_reports_real_total 断言 total>0 且 current<=total",
    "test_repository_orphan_cleanup.py 新增 RepositorySettingNormalizeTests 7 个用例：非法值回退默认、越界 clamp、白名单外回退",
    "pytest tests/test_scan_pdf_degrade.py tests/test_text_scan_incremental.py -q → 9 passed",
    "pytest tests/test_repository_orphan_cleanup.py -q → 13 passed",
    "pytest tests -q 全量 → 194 passed, 32 subtests passed",
    "node --check app.js → 语法通过"
  ],
  "risks": [
    "Library 扫描进度条变为不定动画（无百分比数字），用户无法预知剩余量；这是不预遍历的既定取舍，如需精确进度需恢复预计数（大目录性能代价）",
    "web_window 设置批次不再抛 ValueError，异常值静默回退默认；前端输入合法性仍由各 set_* 白名单/clamp 保证",
    "测试断言 total==0 依赖 Library 扫描「不预遍历」的实现约定，未来若恢复预计数需同步更新断言"
  ],
  "next_steps": [
    "修复 BUG-7（SQLite WAL，repository._connection，可与扫描/缩略图并发写库问题同批）",
    "修复 BUG-8（CBZ 读取缓存目录永不清理，磁盘泄漏）"
  ]
}
```
