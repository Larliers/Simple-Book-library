## 只读审查 + README + bugissue 留档

- 对 `src/` 做只读潜在 bug 评估（Karpathy Guidelines），未改业务代码。
- 重写根目录 `README.md`：功能模块、扫描行为、数据路径、启动/打包/测试说明。
- 新增根目录 `bugissue.md`：8 条待维护 issue（高/中/低优先级 + 建议修复顺序），供下次维护直接开工。

## 验证

- 未运行 pytest（`.venv` 未安装 pytest）。
- 未改 `src/`，`src_construction.md` 无结构变更。
