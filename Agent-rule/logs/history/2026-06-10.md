# 2026-06-10

## Text Novel 规则预览区

- 在 `TextRuleDialog` 中新增规则预览区，展示当前字段规则链对 TXT 样本的最终提取结果。
- 新增 `src/bookhub/library/text_rules/rule_preview.py`，提供自动查找首个 TXT、读取首行/开头文本、构造预览上下文与规则链预览能力。
- 预览样本默认从当前 Text Novel 路径自动取第一个 TXT，并允许用户手动编辑样本路径、第一行和开头文本。
- Settings 打开规则弹窗时传入当前文本预览长度，自动取样读取长度与扫描预览配置保持一致。
- 预览失败时显示失败步骤与错误原因，覆盖非法正则、未匹配、分组越界等规则引擎失败结果。
- 规则 JSON 协议与扫描导入流程保持不变；预览只在 UI 内试算，不写数据库。
- 补齐 `text.rules.preview.*` 中文文案。
- 新增 `src/tests/test_rule_preview.py` 覆盖自动样本、规则链回退、非法正则失败和空目录无样本。
- 同步更新 `src_construction.md` 的更新时间、结构树和组件职责说明。

## 验证

- 通过：`.\.venv\Scripts\python.exe -m py_compile src/bookhub/ui/dialogs/text_rule_dialog.py src/bookhub/ui/pages/settings_page.py src/bookhub/library/text_rules/rule_preview.py src/tests/test_rule_preview.py`
- 通过：`.\.venv\Scripts\python.exe -c "import json; from pathlib import Path; json.loads(Path('src/bookhub/i18n/locales/zh-cn.json').read_text(encoding='utf-8-sig')); print('json_ok')"`
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src/tests -p "test_*.py"`，23 tests OK。

## Text Novel 规则预览区右侧列布局

- 将 `TextRuleDialog` 从“规则链 + 步骤编辑”两列调整为“规则链 + 步骤编辑 + 预览”三列。
- 预览区从步骤编辑区下方移动到最右侧列，规则预览逻辑、规则 JSON 协议与扫描导入流程不变。
- 弹窗默认宽度从 1200 调整为 1320，减少三列布局下的横向挤压。
- 同步更新 `src_construction.md` 中 `text_rule_dialog.py` 的职责说明。

## 验证

- 通过：`.\.venv\Scripts\python.exe -m py_compile src/bookhub/ui/dialogs/text_rule_dialog.py`
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src/tests -p "test_*.py"`，23 tests OK。
- 通过：`git diff --check`，仅有 CRLF 提示。
