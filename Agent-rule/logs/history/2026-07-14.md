## 旧 Widgets UI 源码清理

- 删除无 Web 运行时引用的旧层：`app_window.py`、`pages/`、`widgets/`、`dialogs/`。
- `styles.py` 仅保留 `DEFAULT_FONT_STACK`；移除全局 QSS / `build_app_style`。
- 测试：删除 `test_text_rule_dialog.py`、`test_comic_page_cache.py`；`test_cover_grid_settings.py` 去掉 Widgets UI 用例，保留 Repository/`layout_config`。
- `src_construction.md` 树与注释已改为仅描述 Web UI 事实结构。
- Dev_Document `旧UI-1.0` / `旧UI-2.0` 设计档未动（史料）。

## README 用户向重写

- 按终端用户视角重写根目录 `README.md`：产品定位、功能表、导入/浏览/文本规则、本机数据路径、安装启动、限制说明。
- 弱化开发者内部结构与 Agent 文档；保留可选打包/自检段落供需要的人使用。

## Nuitka 打包（standalone）

- 执行 `.\scripts\build_nuitka.ps1`（约 10 分钟），退出码 0。
- 产物：`build\nuitka\main.dist\main.exe`（约 14 MB 入口；依赖同目录分发）。
