# 2026-06-11 开发记录

## Text Novel 常用正则入口调整

- 根因：上一版将“常用正则”放进 `TextRuleHelpDialog` 左侧栏，但目标位置是主 `TextRuleDialog` 右上角、`使用文档` 按钮左侧。
- 在 `TextRuleDialog` 右上角新增“常用正则”按钮，按钮顺序为“常用正则 / 使用文档”。
- 新增 `TextRuleRegexDialog`，独立展示常用正则表达式示范。
- 将 `TextRuleHelpDialog` 恢复为单列使用文档，不再混放正则示范。
- 正则示范改为“用途 / 示例文本 / 正则 / 提取结果”的浅显格式，避免 Markdown 风格列表。
- 更新 `zh-cn.json`、`src_construction.md` 与 `test_text_rule_dialog.py`。

## 验证

- 通过：`.\.venv\Scripts\python.exe -m py_compile src\bookhub\ui\dialogs\text_rule_dialog.py src\bookhub\ui\dialogs\text_rule_help_dialog.py src\bookhub\ui\dialogs\text_rule_regex_dialog.py src\tests\test_text_rule_dialog.py`
- 通过：`.\.venv\Scripts\python.exe -c "import json; from pathlib import Path; json.loads(Path('src/bookhub/i18n/locales/zh-cn.json').read_text(encoding='utf-8-sig')); print('json_ok')"`
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_text_rule_dialog.py"`，6 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_*.py"`，46 tests OK。
- 通过：`git diff --check`，仅有 CRLF 提示。

## Text Novel 规则步骤新增去除后N行

- 新增 `remove_last_lines` 规则步骤，用于从当前文本末尾删除 N 行。
- `remove_last_lines` 复用 `count` 参数；当 N 大于等于总行数时返回空字符串，非法数量返回结构化失败。
- `TextRuleDialog` 步骤下拉加入“去除后N行”，卡片只显示数量参数。
- 更新 `TextRuleHelpDialog` 与 `zh-cn.json`，补充步骤说明。
- 扩展 `test_rule_engine.py` 与 `test_text_rule_dialog.py`，覆盖执行结果、删除全部行、非法 count 和 UI JSON 写回。
- 同步更新 `src_construction.md` 中规则步骤处理职责说明。

## 验证

- 通过：`.\.venv\Scripts\python.exe -m py_compile src\bookhub\library\text_rules\step_handlers.py src\bookhub\ui\dialogs\text_rule_dialog.py src\bookhub\ui\dialogs\text_rule_help_dialog.py src\tests\test_rule_engine.py src\tests\test_text_rule_dialog.py`
- 通过：`.\.venv\Scripts\python.exe -c "import json; from pathlib import Path; json.loads(Path('src/bookhub/i18n/locales/zh-cn.json').read_text(encoding='utf-8-sig')); print('json_ok')"`
- 通过：`.\.venv\Scripts\python.exe -m unittest src.tests.test_rule_engine src.tests.test_rule_preview`，29 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_text_rule_dialog.py"`，7 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_*.py"`，49 tests OK。
- 通过：`git diff --check`，仅有 CRLF 提示。

## Text Novel 规则弹窗第一轮优化

- 根因：预览区长文本若由普通文本控件参与布局计算，容易把 `TextRuleDialog` 高度撑开；同时单个 step type 下拉已承载过多规则，查找成本高。
- 将步骤选择改为“类别 / 步骤”两级下拉，底层仍保存原有 `{type, params...}` JSON。
- 新增文本清洗/删除/截取类步骤：`normalize_spaces`、`remove_all_spaces`、`normalize_punctuation`、`remove_text`、`remove_regex`、`remove_bracket_content`、`remove_brackets_keep_content`、`take_before_last_text`、`take_after_last_text`。
- 预览结果区保留项目 `TextRulePreviewResult` 状态框与全局滚动条样式，不使用裸系统边框；右侧预览列内部增加纵向 splitter，结果区高度可拖动调整。
- 新增 `text_rule_preview_result_height` 设置项，结果区高度通过 `app_settings` 持久化，取消保存规则也会记住 UI 高度。
- 修复数量参数禁用范围：仅分界线步骤在 `scope=all` 时禁用 `count`，不影响“去除后N行”等普通数量步骤。
- 更新 `zh-cn.json`、`TextRuleHelpDialog`、`src_construction.md` 与相关测试。

## 验证

- 通过：`.\.venv\Scripts\python.exe -c "import json; from pathlib import Path; json.loads(Path('src/bookhub/i18n/locales/zh-cn.json').read_text(encoding='utf-8-sig')); print('json_ok')"`
- 通过：`.\.venv\Scripts\python.exe -m py_compile src\bookhub\library\text_rules\step_handlers.py src\bookhub\library\repository.py src\bookhub\ui\dialogs\text_rule_dialog.py src\bookhub\ui\dialogs\text_rule_help_dialog.py src\bookhub\ui\pages\settings_page.py src\bookhub\ui\app_window.py`
- 通过：`.\.venv\Scripts\python.exe -m unittest src.tests.test_rule_engine src.tests.test_rule_preview`，35 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_text_rule_dialog.py"`，9 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_*.py"`，58 tests OK。
- 通过：`git diff --check`，仅有 CRLF 提示。

## Text Novel 规则步骤新增删除前N行

- 新增 `remove_first_lines` 规则步骤，用于从当前文本开头删除 N 行。
- `remove_first_lines` 复用 `count` 参数；当 N 大于等于总行数时返回空字符串，非法数量返回结构化失败。
- `TextRuleDialog` 将“删除前N行”放入“行处理”分类，卡片只显示数量参数。
- 更新 `TextRuleHelpDialog`、`zh-cn.json` 与 `src_construction.md`。
- 扩展 `test_rule_engine.py` 与 `test_text_rule_dialog.py`，覆盖执行结果、删除全部行、非法 count 和 UI JSON 写回。

## 验证

- 通过：`.\.venv\Scripts\python.exe -c "import json; from pathlib import Path; json.loads(Path('src/bookhub/i18n/locales/zh-cn.json').read_text(encoding='utf-8-sig')); print('json_ok')"`
- 通过：`.\.venv\Scripts\python.exe -m py_compile src\bookhub\library\text_rules\step_handlers.py src\bookhub\ui\dialogs\text_rule_dialog.py src\bookhub\ui\dialogs\text_rule_help_dialog.py src\tests\test_rule_engine.py src\tests\test_text_rule_dialog.py`
- 通过：`.\.venv\Scripts\python.exe -m unittest src.tests.test_rule_engine src.tests.test_rule_preview`，37 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_text_rule_dialog.py"`，10 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_*.py"`，61 tests OK。
- 通过：`git diff --check`，仅有 CRLF 提示。

## Text Novel 规则弹窗新增用户预设

- 新增 Text 规则用户预设能力，预设作为全局 UI 偏好保存到 `app_settings["text_rule_presets"]`，不改 `text_roots.rules_json` 与 `ImportRule`/`RuleStep` 数据结构。
- 支持两类预设：完整规则（`source + steps`）和仅步骤链（`steps`）。
- `TextRuleDialog` 步骤区顶部新增“导入预设 / 保存预设”按钮，底部模板区新增“我的预设”选择、导入和删除操作。
- 完整规则预设导入后会在当前字段新增一条规则；步骤链预设导入后会追加到当前选中规则末尾。
- `AppWindow`/`SettingsPage` 增加预设读取与保存传递链路。
- 更新 `zh-cn.json`、`src_construction.md` 与相关测试。

## 验证

- 通过：`.\.venv\Scripts\python.exe -c "import json; from pathlib import Path; json.loads(Path('src/bookhub/i18n/locales/zh-cn.json').read_text(encoding='utf-8-sig')); print('json_ok')"`
- 通过：`.\.venv\Scripts\python.exe -m py_compile src\bookhub\library\repository.py src\bookhub\ui\app_window.py src\bookhub\ui\pages\settings_page.py src\bookhub\ui\dialogs\text_rule_dialog.py src\tests\test_cover_grid_settings.py src\tests\test_text_rule_dialog.py`
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_text_rule_dialog.py"`，12 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_cover_grid_settings.py"`，6 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest src.tests.test_rule_engine src.tests.test_rule_preview`，37 tests OK。
- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src\tests -p "test_*.py"`，64 tests OK。
- 通过：`git diff --check`，仅有 CRLF 提示。
