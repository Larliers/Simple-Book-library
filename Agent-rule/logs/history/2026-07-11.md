## 埋点清理

- 已移除本次白屏调试的全部 instrumentation（JS fetch/bridge log、Python NDJSON、focus/changeEvent 监听）。
- 保留修复：`setBackgroundColor`、分片渲染、`viewEnter` 无 opacity、同页短路。

---

## 白屏闪烁排查（焦点切回）

- **结论**：焦点切回时**不是**页面刷新。日志仅有 `ActivationChange`/`window.focus`，无伴随 `renderPage` / `push_resources` / `loadFinished`。
- **修复**：`QWebEnginePage.setBackgroundColor` 同步日/夜 `--bg-0`；`html` 不透明底色；`viewEnter` 去掉 opacity:0（减轻切页闪白）。
- 用户确认焦点闪白频率减轻；切页壳层空白另案：漫画瀑布流一次同步 608 卡堵主线程 → `scheduleRenderPage` + 分片渲染。

---

## 切页主线程卡顿（壳层空白）

- 架构：单一 SPA 壳，非每页独立窗口；仅 `#contentArea` 重建。
- 对策：同页 nav 短路；`setTimeout(0)` 先让壳层上色；网格 36/帧分片且 `renderGen` 取消过期任务。

---

## 快添 / 详情封面 / 缩放记忆

- Quick Add 对齐示范：最近标签、书单搜索、「添加/已添加」staging、取消/确认添加。
- 设置「添加文件夹」：`.path-add-btn` 半透明 hover。
- 详情封面：`.detail-cover-slot` + `object-fit: contain`，去掉 2:3 死锁裁切。
- `web_zoom_factor`：启动恢复 + 500ms 轮询 debounce 写回 `app_settings`。
- 验证：`test_web_bridge_smoke.py`；更新 `src_construction.md`。

---

## 示范 HTML：排序控件 + Text Rules 玻璃叠层

仅编辑 `Simple-Book-library-Dev_Document/UI/新UI/glassmorphism-ui.html`，未改 `src/`。

### 任务 A · 排序示范
- **收藏页** page-head 右侧：分段按钮「添加时间：新→旧 / 旧→新」
- **漫画 / 漫画收藏** page-head 右侧：下拉 4 项文件夹排序（日期新旧、名称 A-Z/Z-A）
- 标注「示范，待确认后接入」

### 任务 B · Text Rules 玻璃示范
- 全屏叠层三栏：字段+规则链 | 步骤配置 | 预览/诊断/多样本
- 入口：设置路径行 `Rules`、设置区「打开文本规则示范」、Dialogs 预览内「打开完整文本规则示范」
- 假数据 JS 可切换字段/规则/步骤；底部 Dialogs 精简脚手架保留

### 未做 / 风险
- 未接入真实解析与 `src` Web UI
- 示范确认前勿直接照搬到 `web/`，需对齐真实桥接 API

---

## WebEngine 补回「编辑封面」

对照旧 `library_page._edit_cover`，在玻璃拟态 Web UI 补回非漫画书籍的封面编辑。

### 结论摘要
- 右键菜单与详情栏增加「编辑封面」入口（图书馆/文本/收藏/合集详情等非漫画页）。
- `UiBridge.editCover(resource_id)` → `WebAppWindow.edit_cover`：原生 `QFileDialog` 选图，PIL 写 WebP（无 PIL 则复制原图）到 `img_preview`，再 `update_book_thumbnail_path`；成功后 `reload_data` + `push_resources` + toast。
- i18n：`menu.edit_cover` / `detail.edit_cover` 及 `cover.*` 错误/成功文案写入 `_web_strings` 与 `zh-cn.json`。

### 改动
- `web_bridge.py`：`editCover` slot；strings 键
- `web_window.py`：`edit_cover` 原生选图写缩略图
- `web/js/app.js`：右键与详情栏入口
- `zh-cn.json`：编辑封面相关翻译
- `test_web_bridge_smoke.py`：i18n 键与 slot 存在性
- `src_construction.md`：插入 web 层注释

### 仍待（本轮未做）
- 收藏/漫画页排序 UI
- 设置页漫画性能三项
- 删除路径确认框、扫描摘要面板
- Text Rules Web 化
- 漫画编辑封面（旧版亦仅书籍）

### 验证
- `pytest src/tests/test_web_bridge_smoke.py`：8 passed（venv）


## 示范 HTML：Text Rules 改为原位替换

- 用户反馈：不应把玻璃风 Text Rules 嵌到「打开完整文本规则示范」按钮；应替换 Dialogs Preview 原扁平 Text Rules 区块。
- 已删除全屏叠层与该入口按钮；玻璃三栏直接内嵌 `#textRulesPanel`。
- Settings 路径「Rules」改为滚动定位到 Dialogs 内嵌面板。



对照旧 Qt 页确认缺口后修补：

### 结论摘要
- **文本规则页**：非遗漏，仍故意走原生 `TextRuleDialog`（设置 → 路径 → Rules）。外观仍为 Qt 原生，未玻璃化。
- **合集卡片右键**：已补 Open / Rename / Delete（Web 模态确认）。
- **收藏页右键**：文案改为「移出收藏」；并禁用 Chromium 默认英文菜单。
- **i18n**：扩展 `_web_strings()` 覆盖 `page.count` / 菜单 / 设置选项等，避免英文 fallback；修正 hash 策略值为 `sha256`/`quick`。

### 改动
- `web_window.py`：`QWebEngineView.setContextMenuPolicy(Qt.NoContextMenu)`
- `web/js/app.js`：合集卡片右键；收藏/图书馆菜单修正；全局拦截默认右键；重命名/删除合集模态
- `web_bridge.py`：`renameCollection` / `deleteCollection` / `openFolder`；补齐 strings 键
- `zh-cn.json`：新增合集菜单与设置选项翻译
- `test_web_bridge_smoke.py`：合集 rename/delete 与 i18n 键用例

### 仍待（本轮未做）
- ~~图书馆「编辑封面」~~（已在同日后续条目补回）
- 收藏/漫画页排序 UI
- 设置页漫画性能三项（占位图复制、扫描后自动缩略图、worker 数）
- 删除路径确认框、扫描摘要面板
- Text Rules Web 化

### 验证
- `pytest src/tests/test_web_bridge_smoke.py` 通过


以 `Simple-Book-library-Dev_Document/UI/新UI/glassmorphism-ui.html` 为范本，将 UI 层从纯 QSS 扁平风重写为 `QWebEngineView` 承载的玻璃拟态 Web 前端，`QWebChannel` 双向桥接现有 Python 后端，内建完整日/夜主题引擎与动态交互动效。`library/` 后端与数据结构未改动。

## 新增

- `src/bookhub/ui/web_scheme.py`：`app://` 自定义 scheme；`register_app_scheme()`（QApplication 前调用）、`to_local_path()`、`AppSchemeHandler`（静态资源 + 封面图白名单代理，越权拒绝）。
- `src/bookhub/ui/web_bridge.py`：`UiBridge(QObject)`；Slots 覆盖 bootstrap/搜索/建议/详情/打开/收藏/合集/标签/设置/主题/根目录/扫描/缩略图/字体/错误日志；Signals 推送 resourcesChanged/toast/scanProgress/scanState/settingsChanged/errorLogsChanged/languageChanged；内置库/文本双 `LibraryViewModel`。
- `src/bookhub/ui/web_window.py`：`WebAppWindow`；QWebEngineView + QWebChannel 装配，worker 编排、原生 QFileDialog/TextRuleDialog、字体与设置写库。
- `src/bookhub/ui/web/`：`index.html` + `css/app.css`（玻璃 + 全部动效 + 日/夜主题变量）+ `js/app.js`（路由/渲染/详情/搜索/右键/toast/设置/主题引擎/模态）+ `js/qwebchannel.js`（Qt 官方原样内置）。
- `src/tests/test_web_bridge_smoke.py`：5 条用例（bootstrap 全页、search 载荷、主题持久化、web 资源存在、file:// 归一化）。

## 改动

- `src/main.py`：入口切换为 `WebAppWindow`；创建 QApplication 前设置 `AA_ShareOpenGLContexts` 并注册 `app://`。
- `src/bookhub/i18n/locales/zh-cn.json`：补齐 Web UI 新增 i18n 键（设置项、主题、菜单、详情、通用等）。
- `scripts/build_nuitka.ps1`：新增 `--include-data-dir=src\bookhub\ui\web=...` 与 `--include-qt-plugins=all` 以携带 QtWebEngine 运行时。
- `src_construction.md`：插入 web 层结构与组件说明，更新时间戳与关键实现注释。

## 决策

- 复杂 Text Rules 规则编辑仍复用原生 `TextRuleDialog`（经 Bridge 调起），简单弹窗（Add Tag / Quick Add / New Collection）用 Web 模态重写。
- `PySide6==6.6.1` 元包已内含 QtWebEngine，未新增 pip 依赖。
- 旧 `app_window.py`/`pages/`/`widgets/`/`styles.py` 保留（供复用与回退），未删除。

## 验证

- `pytest src/tests/ -q`：87 passed, 26 subtests passed；新增 web 用例 5 passed。
- offscreen 启动 `WebAppWindow` 冒烟：library 页 6 导航、62 项、封面经 `app://` 正常加载、JS 控制台零报错；截图确认日/夜主题、玻璃质感、设置页两向绑定与中文 i18n 均正常（临时脚本与截图已清理）。

## 下一步 / 未尽 / 风险

- 下一步：验证 Nuitka standalone/onefile 实机不白屏（QtWebEngineProcess/ICU/locales/resources）；漫画大网格惰性加载压测；可选将 Text Rules 也 Web 化。
- 未尽：本轮未执行 Nuitka 实际打包；漫画瀑布流仍一次性渲染（分页已惰性）；en 语言下部分 tr key 走英文兜底（预期）。
- 风险：QtWebEngine + Nuitka 打包为已知易踩坑点；`app://img` 白名单依赖每次资源载荷刷新，若外部直接构造 URL 访问未登记路径将被拒绝（预期行为）。
