# 2026-06-02

## Nuitka 打包准备

- 新增书柜主题应用图标：`src/assets/app_icon_bookcase.svg`、`src/assets/app_icon_bookcase.ico`。
- `src/main.py` 启动时设置应用图标，`src/bookhub/ui/resources/assets.py` 新增顶层资产图标加载能力。
- 新增 `scripts/build_nuitka.ps1`，默认 standalone，传入 `-Onefile` 时构建 onefile；显式包含 `src/assets`、i18n locales、`fitz` 与 `pymupdf` 原始包目录。
- `requirements.txt` 新增固定版本 `Nuitka==4.1.2`；已按依赖安装规则提权安装到 `.venv`。
- `.gitignore` 补齐 Nuitka 产物、`.pyo` 与 `img_preview/.gitkeep` 保留规则。
- 打包排除结论：`scripts/` 与 `src/tests/` 均无运行时代码引用，只用于开发检查/回归测试，不应进入 exe。

## 验证

- 通过：`.\.venv\Scripts\python.exe -m unittest discover -s src/tests -p "test_*.py"`，19 tests OK。
- 初次完整 standalone 构建失败：MinGW/GCC 编译 `module.pymupdf.mupdf.o` 时内存不足。
- 修正：PyMuPDF 不再交给 Nuitka 重编译，改为 `--nofollow-import-to=fitz/pymupdf` + `--include-raw-dir` 携带原始包、预编译 `.pyd` 与 `mupdfcpp64.dll`。
- 修正运行时阻断：新增 `--no-deployment-flag=excluded-module-usage`，避免 Nuitka 因 `--nofollow-import-to` 主动阻止 `fitz` 导入。
- 通过：`powershell -ExecutionPolicy Bypass -File .\scripts\build_nuitka.ps1`，产出 `build\nuitka\main.dist\main.exe`。
- 通过：隔离 `sys.path` 从 dist 导入 `fitz` 并执行 `fitz.open().new_page().get_pixmap()`，确认 PyMuPDF 缩略图核心能力可用。
- 通过：`build\nuitka\main.dist\main.exe --check-pymupdf` 退出码 0，确认 exe 运行时 PyMuPDF 可用。
- 产物检查：未发现 `scripts`、`tests`、运行数据库、扫描日志、缩略图缓存、`Agent-rule`、开发文档目录被带入 dist。
