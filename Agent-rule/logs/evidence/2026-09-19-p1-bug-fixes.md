# 2026-09-19 P1 三项缺陷修复证据

## 范围与隔离

- 仅处理 `BUG-6`、`BUG-8`、`BUG-9`；`BUG-7` 与其余待办未改。
- 基线：干净的 `main@e7aabeb`；未修改数据库 schema 或 Bridge 公共接口。
- 所有测试与 GUI 使用临时数据库、缓存和虚构空书库，未读取真实书库路径或私人数据。

## BUG-6：Qt slot 整型设置归一化

- 红灯：真实 `WebAppWindow.apply_setting(..., "abc")` 对 `comicPageSize`、`viewportBufferScreens`、`gridColumns` 产生 3 个 `ValueError` 子测试失败。
- 根因：Qt slot 在 Repository normalizer 之前残留 `int(value)`，非法值无法到达统一归一化层。
- 修复：三项均原值直传 Repository；保留默认值 48/3/6，`comicPageSize` 仍只触发一次资源刷新。
- 绿灯：非法字符串、`None`、合法值和越界值全部通过；定向文件 `15 passed, 6 subtests passed`。
- 提交：`c060b72 fix: normalize remaining integer settings at repository seam`。

## BUG-9：Vaporwave 字体资源

- 红灯：解析 `fonts.css` 的五个本地 URL，修复前均指向不存在的 `web/css/fonts/`。
- 根因：皮肤 CSS 位于 `css/skins/vaporwave/`，原 `../../fonts/` 少返回一级。
- 修复：五个 URL 改为 `../../../fonts/`；资源测试逐一断言目标存在。
- WebEngine：Vaporwave 在 1440×860、600×749、390×844 中，`document.fonts.check('16px Sora')` 与 `document.fonts.check('16px "Space Mono"')` 均为 true；控制台无 404/错误，各视口 `scrollWidth == clientWidth`。
- 对照：Glass 同三视口无控制台错误或页面水平溢出；元素级“真实字体/强制系统回退字体”比较未新增按钮或卡片截断。390px 顶栏和标题压缩来自既有固定双栏布局，不属于本次字体路径修复。
- 截图：`p1-vaporwave-{1440x860,600x749,390x844}.png` 与 `p1-glass-{1440x860,600x749,390x844}.png` 保存在本次 Codex visualization 目录。
- 提交：`2193f1d fix: resolve vaporwave font assets from web root`。

## BUG-8：CBZ 读取缓存生命周期

- 红灯 1：同一路径 CBZ 修改并改变 `mtime_ns` 后，首次 token 目录仍存在。
- 红灯 2：将无关缓存 marker 设为 31 天前后，再次命中当前缓存时旧目录仍存在。
- 根因：token 虽含路径与 mtime，但旧 marker 只有 mtime，缺少匿名来源身份和任何淘汰生命周期。
- 修复：`.cbz_source` 升级为 v2 JSON（版本、规范化源路径 SHA-1、`mtime_ns`），不保存明文路径；旧纯 mtime marker 命中后升级。成功命中/提取刷新 marker 时间，随后立即删除同源旧 token，并清理超过 30 天的其他直接子目录；跳过当前目录、符号链接和普通文件，单项失败不阻断打开。
- 回归：同源连续修改两次、近期异源保留、31 天过期、旧 marker 升级、损坏 marker 重建、marker 写入权限失败和清理异常降级均通过；`test_new_formats_import.py` 为 `17 passed, 6 subtests passed`。
- 提交：`78e7885 fix: retire stale cbz reader caches`。

## 门禁

- 三组定向 Python：`87 passed, 17 subtests passed in 14.19s`。
- 全量 Python：`279 passed, 54 subtests passed in 36.83s`。
- Node：Quick Add、随机推荐、快捷键、标签管理四组均通过。
- JS 语法：`app.js`、`text_rules.js` 的 `node --check` 通过。
- `git diff --check`：通过；临时 GUI 脚本从 `C:\tmp` 执行后已删除，未进入仓库。
